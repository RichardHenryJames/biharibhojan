# Copy to `.env` for local development (npm run db:up first).
# In Azure, DATABASE_URL is injected as a Container App secret — never commit real secrets.
DATABASE_URL="sqlserver://localhost:1433;database=biharibhojan;user=sa;password=Bihari_Dev_Pw1;encrypt=true;trustServerCertificate=true"
